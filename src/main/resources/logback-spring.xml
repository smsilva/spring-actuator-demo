<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    <include resource="org/springframework/boot/logging/logback/console-appender.xml" />
    <include resource="logging/logback/console-appender-json.xml" />
    <include resource="logging/logback/file-appender-json.xml" />
    <include resource="logging/logback/file-appender-xml.xml" />

<!--    <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd'T'HH:mm:ss.SSSXXX}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr([%X{traceId:-}:%X{spanId:-}]){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>-->
    <property name="TIMESTAMP_FORMAT_WITH_TIMEZONE" value="${TIMESTAMP_FORMAT_WITH_TIMEZONE:-yyyy-MM-dd'T'HH:mm:ss.SSSZZ}"/>
    <property name="LOGS_DIRECTORY" value="${LOGS_DIRECTORY:-/tmp/log}"/>

    <root level="INFO">
        <appender-ref ref="FILE_XML" />
        <appender-ref ref="FILE_JSON" />
        <appender-ref ref="${LOGBACK_CONSOLE_APPENDER:-CONSOLE}" />
    </root>
</configuration>
